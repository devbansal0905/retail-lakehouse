# Screenshots

Add two images here after running the pipeline (see RUN_WITH_DOCKER.md):

- `dashboard.png`  — the Streamlit dashboard at http://localhost:8501
- `pipeline_run.png` — the terminal output of `docker compose up` showing the
  bronze → silver → gold → data-quality stages.

They are referenced from the top-level README so they show on the repo home page.
